# progress
Plan: docs/plans/20260801-install-mac-requires-instance.md
Branch: install-mac-requires-instance
Started: 2026-08-02 12:09:20
---
[2026-08-02 12:18:43] task 1: agb install-config --print-statedir
- modified: agb_ops, tests/test_install_pkg.py, docs/plans/20260801-install-mac-requires-instance.md
- implemented: --print-statedir on install-config as a PURE QUERY. CONFIG_FLAGS + parse_config_args defaults + docstring usage line. Read and raise placed immediately after agb.parse_config(text) and returning, before install_config_values/merge_config_text/write_settings; prints existing.get('statedir') alone, never install_config_values' 'or agb.statedir()' fallback. New PRINT_STATEDIR_ALLOWED = (--print-statedir, --config, --dry-run); the two --print-* flags refused together with their own message, and every other option refused by walking CONFIG_VALUE_ARGS/CONFIG_FLAGS/CONFIG_HOST_ARG. Both refusals live in parse_config_args so a refused argv never opens the config. agb untouched (budget).
- tests: 10 new in tests/test_install_pkg.py - own value on stdout with empty stderr; refused (non-zero, empty stdout) for no statedir key and for an absent config; named file never the default one, both present with different values plus a non-vacuity leg proving the fallback is reachable; a config with a statedir and no mac_id still answers; byte-identical config without --dry-run, parametrized over answering and raising; both --print-* refused together; refusal parametrized over the parser's own tables with non-vacuity on --agb-remote-path/--remote-python/--jump-host/--host/--generate-mac-id; --config and --dry-run accepted end to end.
- validation: python3 -m pytest tests/ -q -> 1910 passed (baseline re-measured at 1900). sh -n install.sh clean. git diff -- agb empty. Mutation-check: 3 mutations, each failing a NAMED test (read below install_config_values -> ..._answers_a_config_that_has_no_mac_id; read at the tail beside --print-mac-id -> ..._leaves_the_config_byte_identical; raise -> return '' -> ..._is_refused_when_the_file_carries_no_statedir), restored and sha256-verified.
- WARNING for later tasks: the first mutation run was VACUOUS. agb_ops is loaded by path through importlib, which caches bytecode and validates on (source mtime in whole seconds, source size); text-move mutations preserve size, so a same-second rewrite reuses the stale __pycache__/agb_ops*.pyc and the test passes against unmutated code. It only revealed itself by flipping 'failed' -> 'passed' across two runs. Any mutation-check here must delete __pycache__/agb_ops*.pyc after writing. Recorded in the plan; belongs in CLAUDE.md Testing conventions in Task 5.
[2026-08-02 12:38:49] task 2: install.sh mac requires --instance
- modified: install.sh, tests/test_install_pkg.py, docs/plans/20260801-install-mac-requires-instance.md
- implemented: install.sh mac now refuses a nameless install (Decision 3's single-quoted message, at the top of role_mac before any filesystem mutation and before probe_farmhost); farm role untouched with the asymmetry commented; the farm-hint --statedir made unconditional (Decision 4) with a comment saying why :618 is deliberately left conditional; install.sh:401-402's withdrawn rule marked withdrawn with its reasoning kept; usage() mac synopsis and --instance entry state REQUIRED
- tests: mac_args gained '--instance': HOST (Decision 5 shape A) with a rewritten docstring; MAC_LABEL/MAC_PLIST constants added; 5 mechanical plist-name repairs; 5 negative assertions naming com.agbridge.plist re-pointed or re-anchored (the two Non-vacuity ones re-anchored on the OTHER instance); test_the_printed_farm_command_omits_the_statedir_when_none_was_given DELETED with a comment naming its Task 3 successor; the two mac-id tests reseed via a new _seed_default_config helper calling agb install-config directly (the installer can no longer write a nameless config); test_a_default_install_... renamed and inverted; test_what_a_default_install_renders_... repaired via --config <install.sh's own DEFAULT_CONFIG spelling>, keeping invariant 14 end-to-end; test_an_absent_instance_... inverted into test_an_absent_instance_never_reaches_the_probe_that_could_name_it with an --instance auto companion; 4 new tests (nameless refused writing nothing, both naming spellings, farm with no --instance, farm defaults its config to the dotfile — the latter keeping install.sh:452's runtime use alive)
- validation: sh -n install.sh; python3 -m pytest tests/ -q -> 1913 passed (baseline 1910, -1 deleted, +4 new); measured refusal-only = 45 failed and refusal+fixture = 12 failed, matching the plan exactly with no thirteenth; four mutation-checks each failed a NAMED test with sha256-verified restore; git diff -- agb empty
[2026-08-02 12:56:40] task 3: install.sh mac adopts its statedir
- modified: install.sh, tests/test_install_pkg.py, docs/plans/20260801-install-mac-requires-instance.md
- implemented: config_given flag recorded at the --instance config derivation (initialised at the top, set -u); the [ -n $statedir ] || die moved out of the sugar block into role_mac, after the header banner and before the first mutation, with [ "$role" = mac ] left in place; a statedir adoption block running 'agb install-config --config $config --print-statedir' against $SELF/agb, only when --config was NOT typed, re-running shell_safe+absolute on the adopted value and announcing 'statedir: adopted X from Y'; usage() --instance and --statedir entries rewritten; output-ordering choice = suppress the adoption's verify_tree report so one verified: line remains and statedir: sits under instance:
- tests: 7 new in tests/test_install_pkg.py -- adopts_the_statedir_from_its_own_config, whose_own_config_carries_no_statedir_is_still_refused, a_new_instance_never_inherits_the_default_configs_statedir, pointed_at_another_config_still_needs_a_statedir (Decision 1), adopted_statedir_is_the_instances_own_never_the_defaults, adopted_from_the_installers_own_tree (order vs copied:), printed_farm_command_carries_the_adopted_statedir (:644's successor); _instance_args and mac_args docstrings corrected
- validation: sh -n install.sh clean; full suite 1913 -> 1920 passed; git diff -- agb empty; 3 mutation-checks each failing a NAMED test (mirror-fallback -> a_new_instance_never_inherits...; delete the die -> whose_own_config_carries_no_statedir...; drop config_given -> pointed_at_another_config...), restored from in-memory snapshot with sha256 verify
[2026-08-02 13:33:43] task 4: verify acceptance criteria
- verified: all 9 criteria EXERCISED end to end in throwaway $HOMEs (scratchpad), stubbed launchctl/ssh/pgrep, real $HOME confirmed untouched. (1) nameless mac install -> exit 1 naming --instance and --instance auto, $HOME still exactly 1 path. (2) --instance auto -> one ssh 'hostname -s', instance farmbox01, its config/plist/logdir. (3) upgrade with no --statedir -> 'statedir: adopted /shared/agb from .../farmbox01/config' under the instance: banner, exit 0, value kept. (4) new instance no --statedir -> exit 1, $HOME path count unchanged 24->24. (5) --instance hostb --config <the default config carrying statedir=/shared/DEFAULT> no --statedir -> exit 1, that config byte-identical (sha256); same argv WITH --statedir installs. (6) install.sh farm no --instance -> exit 0, config + 4 hook events; farm --instance still refused. (7) legacy: v0.5.0 extracted from the tag and ITS OWN installer run nameless; today's agb instances -> '(default) com.agbridge <config>' and --labels -> com.agbridge; agb-refresh --config binds com.agbridge with NO 'no plist names this config' note, negative control (plist moved) DOES print it; bare agb-refresh swept it and really forgot a binding (forgot 1 of 1, map empty after); agb close-done found it; pre-0.5.0 flagless-plist branch exercised too. (8) git diff -- agb empty vs HEAD, vs 598d184 and vs v0.6.0; 103198 CHARACTERS (103212 wc -c bytes) vs AGB_PARSE_BUDGET 103200 strict < -> 1 char headroom, unmoved. (9) 1921 passed, sh -n install.sh and sh -n agb-refresh clean.
- skipped: none. No criterion needed a Mac -- --no-load plus a recording ssh stub is the same machinery tests/test_install_pkg.py's mac_args/_ssh_answering use.
- fixed: no defects found; no production code changed. git diff -- install.sh, agb_ops, agb-refresh, agb all empty.
- validation: full suite 1921 passed (1920 before, +1 new test); sh -n install.sh; sh -n agb-refresh; mutation-check of the new guard (instance_display_name's 'or "(default)"' removed -> the named test fails), in-memory snapshot restored and sha256-verified, __pycache__ dropped either side.
- added: tests/test_install_pkg.py::test_the_installed_tree_still_reads_a_plist_it_can_no_longer_write -- the criterion-7 conjunction had no test; it renders the nameless plist from dist/com.agbridge.plist (the shipped template, so it cannot drift from a real 0.5.0 Mac) and reads it back with the agb the installer just installed, asserting the NAME COLUMN is (default) and --labels lists it.
